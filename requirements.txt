# Core dependencies - minimal by design
# Install with: pip install -r requirements.txt

# For OpenAI support (optional)
# openai>=1.0.0

# For YAML support (optional)
# pyyaml>=5.4

# For validation (optional but recommended)
# pydantic>=2.0

# Note: This package has minimal core dependencies
# Install optional dependencies with:
# pip install python-prompt-manager[openai]
# pip install python-prompt-manager[all]